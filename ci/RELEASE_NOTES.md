# Devouring Storms: The Point of No Return — 1.9.128

- **Config/UI revamp, phase 1**: the Storm Config screen (⚡ button on the
  main menu) gets the MCSM cinematic treatment — a deep violet-night plate
  with a faint horizon glow behind every tab and picker, thin blue-violet
  accent edges framing the whole screen (including the 3D Storm Preview),
  and a "⚡ DEVOURING STORMS · storm config" brand tag above the tabs.
  All tabs, sliders, buttons and the preview work exactly as before.
- MCSM main menu, coloured dynamic lights, top-left hotbar, built-in
  Story Look pack — all from previous builds.

Remove ALL older jars from `mods/`; install only
`devouringstorms-1.9.128-26.2-beta-ds.jar`.
