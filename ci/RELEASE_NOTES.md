# Devouring Storms: The Point of No Return — 1.9.127

- **MCSM-style main menu, phase 1**: the vanilla panorama is replaced by
  a dark cinematic plate — deep violet night gradient, storm glow on the
  horizon, 120 softly twinkling stars. A near-opaque top strip carries a
  big scaled **DEVOURING STORMS** logo with "THE POINT OF NO RETURN"
  beneath it, and a matching bottom bar shows the episode tagline and
  build stamp. The base mod's Storm Config / 3D Storm Preview buttons
  (top-right) stay.
- Coloured dynamic lights complete, top-left MCSM hotbar, built-in
  Story Look pack — all from previous builds.

Remove ALL older jars from `mods/`; install only
`devouringstorms-1.9.127-26.2-beta-ds.jar`.
