# Devouring Storms: The Point of No Return — 1.9.126

- **Coloured dynamic lights COMPLETE (phase 2b)**: a dropped luminous
  item now lights the GROUND around it through the real light engine —
  a torch drop lights the cave like a placed torch (level 14), glowstone
  and lava buckets at 15, flint & steel a steel-blue glint at 8. The
  light follows the item as it slides and cleans itself up the moment
  the drop is picked up, merges or despawns. It never destroys a real
  block — only air is ever replaced.
- All three phases together: coloured motes (1.9.124), self-lit item
  render (1.9.125), real terrain light (1.9.126).

Remove ALL older jars from `mods/`; install only
`devouringstorms-1.9.126-26.2-beta-ds.jar`.
