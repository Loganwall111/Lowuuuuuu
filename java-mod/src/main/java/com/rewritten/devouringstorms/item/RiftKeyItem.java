package com.rewritten.devouringstorms.item;

import net.minecraft.world.item.Item;

/**
 * THE RIFT KEY. Forged from the Watcher's own gaze and an impossible memory.
 * Offer it to a Terminal standing on a Mainframe foundation — and the portal opens.
 */
public class RiftKeyItem extends Item {

    public RiftKeyItem(Properties props) {
        super(props);
    }
}
