package com.rewritten.devouringstorms.block;

import net.minecraft.world.level.block.JukeboxBlock;

/**
 * THE DECAYED JUKEBOX. It still works. Mostly.
 * Plays the Devouring Storms discs — and, as the archives say, "also supports Vanilla discs."
 * (Behaviour inherited from the vanilla jukebox; the difference is entirely in how it sounds
 *  coming out of this one.)
 */
public class DecayedJukeboxBlock extends JukeboxBlock {

    public DecayedJukeboxBlock(Properties props) {
        super(props);
    }
}
