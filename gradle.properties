# Gradle
org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true

# Fabric
# These versions were reconstructed from the original jar's build manifest
# (META-INF/MANIFEST.MF) so a rebuild reproduces the same binary as closely as possible.
minecraft_version=26.2
loader_version=0.19.3
# Fabric API for Minecraft 26.2 (latest 26.2 release).
fabric_version=0.158.0+26.2

# Mod
mod_version=1.9.60-26.2-beta
maven_group=net.dabicco
archives_base_name=dabywitherstormmod
